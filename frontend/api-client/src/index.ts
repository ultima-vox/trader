// Generated client entry point.
//
// Application code imports from here and wraps `VoxClient` in a service; it never calls
// fetch against the API directly and never re-declares a DTO.
export * from "./types";
export * from "./client";
